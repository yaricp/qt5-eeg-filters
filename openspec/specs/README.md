# Specs

Current source-of-truth capability specs for qt5-eeg-filters.

Each capability lives in `<capability>/spec.md` and describes WHAT the
system does (requirements with scenarios). Specs are updated by archiving
completed changes from `openspec/changes/` (`/opsx:archive` or
`openspec archive <change-id>`), not edited ad hoc.
